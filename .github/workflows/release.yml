name: release
on:
  push:
    tags:
    - 'v*.*.*'

jobs:

  release:
    name: Release
    runs-on: ubuntu-latest
    steps:

    - name: Check out code
      uses: actions/checkout@v1

    - name: Get Release by Tag
      id: get_release_by_tag
      uses: jonfriesen/get-release-by-tag@v0.0.11
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}

    - name: TESTING
      run: git log $(git describe --tags --abbrev=0)..HEAD --pretty=format:"%h %s"

    - name: Set environment variable with changes since last tag
      run: echo ::set-env name=CHANGELOG::$(git log $(git describe --tags --abbrev=0)..HEAD --pretty=format:"%h %s")

    - name: Publish to Registry
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: jonfriesen/sample-website
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        dockerfile: Dockerfile
        tags: "latest,${{ steps.get_release_by_tag.outputs.formatted_tag }}"